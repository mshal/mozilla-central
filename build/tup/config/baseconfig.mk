# This is a stub file used by tup when a Tupfile includes autoconf.mk directly.
# The autoconf.mk file includes config/baseconfig.mk, which tup can't parse,
# and isn't actually needed for this backend.
